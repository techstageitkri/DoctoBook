const permissions = [
  "auth.session.revoke",
  "clinic.read",
  "clinic.manage",
  "doctor.approve",
  "appointment.book",
  "appointment.manage",
  "payment.manage",
  "refund.manage",
  "review.moderate",
  "audit.read"
];

console.log(`Seed placeholder ready for ${permissions.length} initial permissions.`);
